"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findMissingFieldNames = exports.hasFields = void 0;
function hasFields(obj, fields) {
    const inObj = (object) => fields.every((f) => object[f]);
    if (Array.isArray(obj)) {
        return obj.every((o) => inObj(o));
    }
    return inObj(obj);
}
exports.hasFields = hasFields;
const findMissingFieldNames = (obj, fields) => fields.reduce((prev, curr) => (obj[curr] ? prev : `${prev.length ? `${prev},` : ''}${curr}`), '');
exports.findMissingFieldNames = findMissingFieldNames;
//# sourceMappingURL=typeHelpers.js.map